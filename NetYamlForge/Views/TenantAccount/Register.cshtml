@* 用户注册页面 *@
@using NetYamlForge.Localization
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResource> L
@model NetYamlForge.Models.Auth.RegisterViewModel
@{
    ViewData["Title"] = L["TenantUserRegister"];
    Layout = "_Layout";
}

<div class="min-h-screen flex items-center justify-center bg-base-200 py-12 px-4">
    <div class="max-w-md w-full">
        @* 页面标题 *@
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold">@L["TenantUserRegister"]</h1>
            <p class="text-base-content/60 mt-2">@L["TenantCreateYourAccount"]</p>
        </div>

        @* 注册表单卡片 *@
        <div class="card bg-base-100 shadow-xl">
            <div class="card-body">
                <form method="post" class="space-y-4">
                    <input type="hidden" name="project" value="@ViewData["ProjectName"]" />
                    <input type="hidden" name="token" value="@ViewData["Token"]" />
                    
                    @* 用户名 *@
                    <div class="form-control">
                        <label class="label">
                            <span class="label-text font-medium">@L["UserName"] <span class="text-error">*</span></span>
                        </label>
                        <input asp-for="UserName" 
                               class="input input-bordered" 
                               placeholder="@L["TenantPleaseEnterUserNameLimit"]" />
                        <span asp-validation-for="UserName" class="text-error text-sm mt-1"></span>
                    </div>
                    
                    @* 显示名称 *@
                    <div class="form-control">
                        <label class="label">
                            <span class="label-text font-medium">@L["TenantDisplayName"] <span class="text-error">*</span></span>
                        </label>
                        <input asp-for="DisplayName" 
                               class="input input-bordered" 
                               placeholder="@L["TenantPleaseEnterDisplayName"]" />
                        <span asp-validation-for="DisplayName" class="text-error text-sm mt-1"></span>
                    </div>
                    
                    @* 密码 *@
                    <div class="form-control">
                        <label class="label">
                            <span class="label-text font-medium">@L["Password"] <span class="text-error">*</span></span>
                        </label>
                        <input asp-for="Password" 
                               class="input input-bordered" 
                               type="password"
                               placeholder="@L["TenantPleaseEnterPasswordLimit"]" />
                        <span asp-validation-for="Password" class="text-error text-sm mt-1"></span>
                    </div>
                    
                    @* 确认密码 *@
                    <div class="form-control">
                        <label class="label">
                            <span class="label-text font-medium">@L["TenantConfirmPassword"] <span class="text-error">*</span></span>
                        </label>
                        <input asp-for="ConfirmPassword" 
                               class="input input-bordered" 
                               type="password"
                               placeholder="@L["TenantPleaseEnterPasswordAgain"]" />
                        <span asp-validation-for="ConfirmPassword" class="text-error text-sm mt-1"></span>
                    </div>
                    
                    @* 邮箱 *@
                    <div class="form-control">
                        <label class="label">
                            <span class="label-text font-medium">@L["TenantEmail"]</span>
                        </label>
                        <input asp-for="Email" 
                               class="input input-bordered" 
                               type="email"
                               placeholder="example@email.com" />
                        <span asp-validation-for="Email" class="text-error text-sm mt-1"></span>
                    </div>
                    
                    @* 电话 *@
                    <div class="form-control">
                        <label class="label">
                            <span class="label-text font-medium">@L["TenantPhone"]</span>
                        </label>
                        <input asp-for="Phone" 
                               class="input input-bordered" 
                               type="tel"
                               placeholder="13800138000" />
                        <span asp-validation-for="Phone" class="text-error text-sm mt-1"></span>
                    </div>
                    
                    @* 验证错误汇总 *@
                    <div asp-validation-summary="ModelOnly" class="text-error text-sm bg-error/10 p-3 rounded"></div>
                    
                    @* 注册按钮 *@
                    <button type="submit" class="btn btn-primary w-full">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z" />
                        </svg>
                        @L["Register"]
                    </button>
                </form>
                
                <div class="divider">@L["TenantOr"]</div>
                
                @* 登录链接 *@
                <div class="text-center">
                    <p class="text-sm">
                        @L["TenantAlreadyHaveAccount"]
                        <a asp-action="Login" class="link link-primary">@L["TenantLoginNow"]</a>
                    </p>
                </div>
            </div>
        </div>
        
        @* 页脚信息 *@
        <div class="text-center mt-6 text-xs text-base-content/60">
            <p>@L["TenantRegisterConsent"]</p>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
