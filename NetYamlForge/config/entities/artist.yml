entities:
  artist:
    table: Artist
    key: ArtistId
    displayName: Artist
    softDelete: false
    paging:
      pageSize: 10
      mode: numbered
    layout:
      forms:
        columns: 1
        order:
        - Name
      filters:
        columns: 1
        order: []
    columns:
      ArtistId:
        type: int
        identity: true
        label: ID
        searchable: false
        sortable: true
        labelKey: config.entities.artist.entities.artist.columns.ArtistId.label
      Name:
        type: string
        label: Name
        searchable: true
        sortable: true
        labelKey: config.entities.artist.entities.artist.columns.Name.label
    forms:
      Name:
        type: string
        label: Name
        editable: true
        labelKey: config.entities.artist.entities.artist.forms.Name.label
    filters: {}
    displayNameKey: config.entities.artist.entities.artist.displayName
