entities:
  t_selection:
    table: t_selection
    key: t_selection_id
    displayName: T_SELECTION
    columns:
      ad_pinstance_id: {type: decimal}
      t_selection_id: {type: decimal, identity: true}
      viewid: {type: string}
      t_selection_uu: {type: string}
