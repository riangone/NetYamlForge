entities:
  post_tag:
    table: 'post_tags'
    key: 'id'
    displayName: 'Post Tag'
    displayNameKey: 'entities.post_tag.displayName'
    softDelete: false
    paging:
      pageSize: 20
      mode: numbered
    layout:
      forms:
        columns: 2
        order: ['post_id', 'tag_id']
      filters:
        columns: 4
        order: []
    columns:
      id: { type: int, identity: true, label: 'Id', labelKey: 'entities.post_tag.columns.id.label', sortable: true }
      post_id: { type: int, required: true, label: 'Post Id', labelKey: 'entities.post_tag.columns.post_id.label', sortable: true }
      tag_id: { type: int, required: true, label: 'Tag Id', labelKey: 'entities.post_tag.columns.tag_id.label', sortable: true }
    forms:
      post_id:
        type: int
        required: true
        label: 'Post Id'
        labelKey: 'entities.post_tag.forms.post_id.label'
        editable: true
        foreignKey:
          entity: post
          displayColumn: 'title'
      tag_id:
        type: int
        required: true
        label: 'Tag Id'
        labelKey: 'entities.post_tag.forms.tag_id.label'
        editable: true
        foreignKey:
          entity: tag
          displayColumn: 'name'
    filters: {}
    links: {}
