@* ファイル概要: salesforce-crm 専用レイアウトのエントリポイントです。
   まずは共通レイアウトを親として利用し、将来のプロジェクト専用UI拡張の受け皿にします。 *@
@{
    Layout = "/Views/Shared/_Layout.cshtml";
}

@RenderBody()

@section Styles {
    @RenderSection("Styles", required: false)
}

@section Scripts {
    @RenderSection("Scripts", required: false)
}
