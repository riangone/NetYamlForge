title: 担当者パフォーマンス
description: "セールス担当者の個人 KPI、担当リード、フォローアップアクション、活動履歴を管理"

sections:
  # ─────────────────────────────────────────
  # 個人 KPI カード
  # ─────────────────────────────────────────
  - id: my_kpi
    title: 自分の KPI（今月）
    visibleToRoles: [sales_rep, sales_manager]
    component: stat_cards
    sourceType: custom
    source: |
      SELECT '今月新規リード' AS metric_name, CAST(COUNT(*) AS TEXT) AS metric_value, '🆕' AS metric_icon, NULL AS metric_delta
      FROM sales_leads WHERE assigned_sales = @currentUser AND strftime('%Y-%m', created_at) = strftime('%Y-%m', 'now')
      UNION ALL
      SELECT '今月成約', CAST(COUNT(*) AS TEXT), '🏆', NULL
      FROM sales_leads WHERE assigned_sales = @currentUser AND status = 'won' AND strftime('%Y-%m', updated_at) = strftime('%Y-%m', 'now')
      UNION ALL
      SELECT '停滞リード', CAST(COUNT(*) AS TEXT), '⚠️', NULL
      FROM sales_leads WHERE assigned_sales = @currentUser AND status NOT IN ('won', 'lost') AND (last_contact_at IS NULL OR CAST((JULIANDAY('now') - JULIANDAY(last_contact_at)) AS INTEGER) >= 7)
      UNION ALL
      SELECT 'フォロー予定（今週）', CAST(COUNT(*) AS TEXT), '📅', NULL
      FROM lead_activities WHERE created_by = @currentUser AND next_action IS NOT NULL AND next_action_date IS NOT NULL AND date(next_action_date) BETWEEN date('now') AND date('now', '+7 days')
    columns: [metric_name, metric_value, metric_icon, metric_delta]
    labelField: metric_name
    valueField: metric_value
    iconField: metric_icon
    deltaField: metric_delta
    pageSize: 4
    editable: false
    readOnly: true

  - id: ai_morning_briefing
    title: '🌅 今日のAI推奨アクション TOP5'
    visibleToRoles: [sales_rep, sales_manager]
    component: table
    sourceType: custom
    source: |
      SELECT
        c.name AS 顧客名,
        CASE t.task_type
          WHEN 'email'       THEN '✉️ メール送信'
          WHEN 'call'        THEN '📞 電話'
          WHEN 'appointment' THEN '📅 アポ取得'
          WHEN 'test_drive'  THEN '🚗 試乗誘致'
          WHEN 'catalog'     THEN '📄 資料送付'
          ELSE t.task_type
        END AS アクション,
        COALESCE(SUBSTR(t.ai_recommendation, 1, 60), '-') AS AI推奨理由,
        COALESCE(t.due_date, '期限なし') AS 期限,
        COALESCE(l.lead_id, '') AS リードID
      FROM lead_nurturing_tasks t
      LEFT JOIN customers c ON t.customer_id = c.customer_id
      LEFT JOIN sales_leads l ON t.lead_id = l.lead_id
      WHERE t.status IN ('pending', 'in_progress')
      ORDER BY t.priority_score DESC, t.due_date ASC
      LIMIT 5
    columns: [顧客名, アクション, AI推奨理由, 期限, リードID]
    pageSize: 5
    editable: false
    readOnly: true

  # ─────────────────────────────────────────
  # 担当ホットリード
  # ─────────────────────────────────────────
  - id: my_hot_leads
    title: 🔥 自分のホットリード（スコア 70+）
    visibleToRoles: [sales_rep, sales_manager]
    component: table
    sourceType: custom
    source: |
      SELECT l.lead_id AS ID,
             c.name AS 顧客名,
             CASE WHEN l.budget IS NOT NULL THEN '¥' || printf('%,.0f', l.budget) ELSE '−' END AS 予算,
             l.lead_score AS スコア,
             CASE l.status
               WHEN 'new'       THEN '🆕 新規'
               WHEN 'contacted' THEN '📞 接触済'
               WHEN 'qualified' THEN '💼 商談中'
               WHEN 'proposal'  THEN '📋 提案中'
               ELSE l.status
             END AS ステータス,
             CASE WHEN l.last_contact_at IS NOT NULL
                  THEN CAST(CAST((JULIANDAY('now') - JULIANDAY(l.last_contact_at)) AS INTEGER) AS TEXT) || '日前'
                  ELSE '未接触'
             END AS 最終接触
      FROM sales_leads l
      INNER JOIN customers c ON l.customer_id = c.customer_id
      WHERE l.assigned_sales = @currentUser
        AND l.lead_score >= 70
        AND l.status NOT IN ('won', 'lost')
      ORDER BY l.lead_score DESC
      LIMIT 5
    columns: [ID, 顧客名, 予算, スコア, ステータス, 最終接触]
    pageSize: 5
    actions:
      - label: 対応
        url: "/auto-dealer-demo/DynamicEntity/Edit?entity=sales_leads&id={ID}"
        class: "btn-primary"

  # ─────────────────────────────────────────
  # フォローアップリマインダー（今週期限）
  # ─────────────────────────────────────────
  - id: followup_reminders
    title: 📅 フォローアップリマインダー（今週期限）
    visibleToRoles: [sales_rep, sales_manager]
    component: table
    sourceType: custom
    source: |
      SELECT a.activity_id AS ID,
             l.lead_id AS リードID,
             c.name AS 顧客名,
             a.next_action AS 次のアクション,
             a.next_action_date AS 期限,
             CASE
               WHEN date(a.next_action_date) < date('now') THEN '🔴 期限超過'
               WHEN date(a.next_action_date) = date('now') THEN '🟠 今日'
               ELSE '🟢 今週'
             END AS 緊急度
      FROM lead_activities a
      INNER JOIN sales_leads l ON a.lead_id = l.lead_id
      INNER JOIN customers c ON l.customer_id = c.customer_id
      WHERE a.created_by = @currentUser
        AND a.next_action IS NOT NULL
        AND a.next_action_date IS NOT NULL
        AND date(a.next_action_date) <= date('now', '+7 days')
        AND l.status NOT IN ('won', 'lost')
      ORDER BY a.next_action_date ASC
      LIMIT 5
    columns: [ID, リードID, 顧客名, 次のアクション, 期限, 緊急度]
    pageSize: 5
