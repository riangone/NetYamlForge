# ── 图片自动分析/背单词提取 CLI 优先级链 (CLI Chain) ────────────────────────
# 上传图片后会依次尝试以下 CLI 生成图片描述（Caption）与背单词素材（Words）：
#   opencode cli → antigravity cli → claude code cli
# 均为订阅制的本机 CLI（非按量计费 API），任意一个登录失效/超时/限流，
# 会自动 fallback 到下一个，避免像 antigravity OAuth 过期那样静默失效。
#
# 整体开关：设为 false 可完全关闭图片自动分析功能（"设置为可选"）。
# 默认: true
CLI_CHAIN_ENABLED=true

# 优先顺序，逗号分隔，可裁剪/调整（合法值: opencode, antigravity, claude）。
# 留空则使用默认顺序: opencode,antigravity,claude
CLI_CHAIN_ORDER=opencode,antigravity,claude
