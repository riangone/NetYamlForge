# 联系人管理系统

このディレクトリは `--init-project` で生成された最小テンプレートです。

## 次の作業

1. `project.yaml` の DB 設定を確認
2. `dotnet run -- --scaffold-entities --project=contact-manager` を実行
3. `entities/` と `pages/` に必要な YAML を追加
4. `/contact-manager` と `/contact-manager/Dashboard` を確認
