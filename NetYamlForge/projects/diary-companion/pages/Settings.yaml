title: Settings
titleKey: projects.diary-companion.pages.Settings.title
isPublic: true

ui:
  page:
    layout: single
    density: comfortable

sections:
  - id: settings_section
    titleKey: projects.diary-companion.pages.Settings.sections.settings_section.title
    sourceType: custom
    source: "SELECT 1 AS DummyId"
    columns: [DummyId]
    pageSize: 1
    editable: false
    readOnly: true
    ui:
      component: DataTable
